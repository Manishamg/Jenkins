pipeline {
    agent {label "node2"}
    stages{
        stage ("AutoTest"){
            steps {
            echo "Testing slave server with automation"
            }
        }
    }
    post{
        success {
            echo "success"
        }
        failure {
            echo "failure"
        }
    }
}
